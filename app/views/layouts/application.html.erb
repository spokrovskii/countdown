<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= content_for?(:title) ? yield(:title) : "Countdown" %></title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav>
      <% if user_signed_in? %>
        Currently signed in as: <%= link_to "#{current_user.email}", edit_user_registration_path(@user) %> |
        <%= link_to 'Sign Out', destroy_user_session_path, :method => 'delete' %>
      <% else %>
        <%= link_to 'Sign Up', new_user_registration_path %> | <%= link_to 'Sign In', new_user_session_path %>
      <% end %>
    </nav>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<%= render "application/flash" %>

  <div class="row">
    <div class="columns content">
      <%= yield %>
    </div>
  </div>

  <% if Rails.env.test? %>
    <%= hidden_field_tag :testing, true  %>
  <% end %>
</body>
</html>
